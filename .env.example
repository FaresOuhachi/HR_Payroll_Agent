# =============================================================================
# Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# CONCEPT: Environment variables separate configuration from code.
# This follows the "12-Factor App" methodology:
#   - Secrets (API keys, passwords) never live in source code
#   - Different environments (dev/staging/prod) use different .env files
#   - pydantic-settings reads these automatically via BaseSettings
# =============================================================================

# --- Database (PostgreSQL) ---
DATABASE_URL=postgresql+asyncpg://hr_admin:hr_secret_2025@localhost:5432/hr_payroll

# --- Redis ---
REDIS_URL=redis://localhost:6379/0

# --- OpenAI API ---
OPENAI_API_KEY=sk-your-openai-api-key-here

# --- JWT Authentication ---
# Generate a secret: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-me-to-a-random-secret-key
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- LangSmith (LLM Observability) ---
# Sign up at https://smith.langchain.com to get an API key
LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://eu.api.smith.langchain.com
LANGSMITH_API_KEY=lsv2_pt_your-langsmith-api-key-here
LANGSMITH_PROJECT=RH Payroll Agent

# --- Application ---
APP_NAME=HR Payroll Agent
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO
